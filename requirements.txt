Django==4.2.17
python-decouple==3.8
# Pillow is optional - only needed for product images
# Install manually if needed: python -m pip install Pillow
# Note: Pillow requires Python 3.8-3.13 on Windows
